import Head from "next/head";
import Link from "next/link";
import { useSelector } from "react-redux";
import { useRouter } from "next/router";
import { useEffect } from "react";
import { STAGE2_MODULES } from "../../../../../Data/Reasoning/stage2Modules";

export default function Stage2Verbal(){const router=useRouter();const {user,isAuthenticated}=useSelector((state)=>state.auth);useEffect(()=>{if(!isAuthenticated&&user===null)return;if(!user?.userId)router.replace("/Auth");},[isAuthenticated,user,router]);if(!router.isReady||!isAuthenticated||!user?.userId)return null;const modules=STAGE2_MODULES.verbal;return <><Head><title>Stage 2 · Critical Reading & Argument | Upskilleduonline</title></Head><main style={{minHeight:"80vh",padding:"48px 20px 80px",background:"var(--surface)"}}><div style={{maxWidth:1150,margin:"0 auto"}}><Link href="/Reasoning/Verbal/Dashboard" style={{color:"var(--muted)",textDecoration:"none",fontWeight:700}}>← Back to Verbal Dashboard</Link><p style={{marginTop:28,color:"var(--blue)",fontWeight:800,letterSpacing:".08em",textTransform:"uppercase",fontSize:13}}>Stage 02 · Level 1 · Grade 3</p><h1 style={{fontSize:"clamp(34px,5vw,54px)",lineHeight:1.07,margin:0}}>Critical Reading & Argument</h1><p style={{maxWidth:760,color:"var(--muted)",fontSize:18,lineHeight:1.7}}>Move from basic comprehension to structured critical reading. Learn to map arguments, identify assumptions, compare viewpoints and evaluate evidence.</p><div style={{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(300px,1fr))",gap:18,marginTop:30}}>{modules.map((module,index)=><article key={module.id} style={{background:"var(--card)",border:"1px solid var(--border)",borderRadius:18,padding:24}}><div style={{color:"var(--blue)",fontWeight:800,fontSize:13}}>MODULE {String(index+1).padStart(2,"0")}</div><h2 style={{color:"var(--text)",fontSize:21}}>{module.title}</h2><p style={{color:"var(--muted)",lineHeight:1.6}}>{module.focus}</p><span style={{display:"inline-block",marginTop:8,padding:"6px 10px",borderRadius:99,background:"#f2f4f7",color:"#667085",fontSize:12,fontWeight:800}}>Curriculum built · Activities next</span></article>)}</div><p style={{marginTop:28,color:"var(--muted)",lineHeight:1.6}}>Stage 2 is intentionally introduced as a separate curriculum layer. Its interactive Question Bank and activity connections will be added next without changing the completed Stage 1 learning flow.</p></div></main></>}
